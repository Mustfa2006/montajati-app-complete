# 📱 نظام التنقل من الإشعار إلى الطلب

## 🎯 الهدف

عندما ينقر المستخدم على الإشعار، يجب أن ينتقل مباشرة إلى صفحة تفاصيل الطلب الخاص به.

---

## 🔄 سير العملية الكاملة

### 1️⃣ إرسال الإشعار من Backend

```javascript
// backend/services/firebase_admin_service.js
async sendOrderStatusNotification(fcmToken, orderId, newStatus, customerName = '') {
  const message = {
    token: fcmToken,
    notification: {
      title: '🚗 قيد التوصيل',
      body: 'أحمد محمد - (قيد التوصيل)'
    },
    data: {
      type: 'order_status_update',
      orderId: orderId.toString(),  // ✅ معرف الطلب
      newStatus: newStatus,
      customerName: customerName,
      timestamp: new Date().toISOString(),
      click_action: 'FLUTTER_NOTIFICATION_CLICK'
    }
  };

  return await this.messaging.send(message);
}
```

### 2️⃣ استقبال الإشعار في Flutter

```dart
// frontend/lib/services/fcm_service.dart

// السيناريو 1: التطبيق في المقدمة
FirebaseMessaging.onMessage.listen(_handleForegroundMessage);

Future<void> _handleForegroundMessage(RemoteMessage message) async {
  debugPrint('📱 تم استلام إشعار في المقدمة');
  debugPrint('📊 البيانات: ${message.data}');
  
  // عرض الإشعار محلياً
  await _showLocalNotification(message);
}

// السيناريو 2: التطبيق في الخلفية (النقر على الإشعار)
FirebaseMessaging.onMessageOpenedApp.listen(_handleMessageOpenedApp);

void _handleMessageOpenedApp(RemoteMessage message) {
  debugPrint('👆 تم النقر على الإشعار');
  _processNotificationData(message.data);
}

// السيناريو 3: التطبيق مغلق (النقر على الإشعار)
Future<void> _handleInitialMessage() async {
  final initialMessage = await _messaging?.getInitialMessage();
  if (initialMessage != null) {
    debugPrint('🚀 تم فتح التطبيق من إشعار');
    _processNotificationData(initialMessage.data);
  }
}
```

### 3️⃣ معالجة بيانات الإشعار والتنقل

```dart
/// معالجة بيانات الإشعار والتنقل إلى الصفحة المناسبة
void _processNotificationData(Map<String, dynamic> data) {
  debugPrint('📊 معالجة بيانات الإشعار: $data');

  try {
    // استخراج معرف الطلب من البيانات
    final orderId = data['orderId'] ?? data['order_id'];
    final notificationType = data['type'] ?? 'order_status_update';

    debugPrint('📋 نوع الإشعار: $notificationType');
    debugPrint('🔗 معرف الطلب: $orderId');

    if (orderId != null && orderId.toString().isNotEmpty) {
      // ✅ التنقل إلى صفحة تفاصيل الطلب
      _navigateToOrderDetails(orderId.toString());
    } else {
      debugPrint('⚠️ لم يتم العثور على معرف الطلب');
    }
  } catch (e) {
    debugPrint('❌ خطأ في معالجة بيانات الإشعار: $e');
  }
}

/// التنقل إلى صفحة تفاصيل الطلب
void _navigateToOrderDetails(String orderId) {
  try {
    debugPrint('🚀 بدء التنقل إلى صفحة الطلب: $orderId');

    // الحصول على BuildContext من GoRouter
    final context = AppRouter.router.routerDelegate.navigatorKey.currentContext;

    if (context == null) {
      debugPrint('⚠️ لم يتم العثور على BuildContext - سيتم المحاولة لاحقاً');
      // محاولة التنقل بعد تأخير قصير
      Future.delayed(const Duration(milliseconds: 500), () {
        final ctx = AppRouter.router.routerDelegate.navigatorKey.currentContext;
        if (ctx != null && ctx.mounted) {
          _performNavigation(ctx, orderId);
        }
      });
      return;
    }

    _performNavigation(context, orderId);
  } catch (e) {
    debugPrint('❌ خطأ في التنقل: $e');
  }
}

/// تنفيذ التنقل الفعلي
void _performNavigation(BuildContext context, String orderId) {
  try {
    debugPrint('🔗 التنقل إلى صفحة الطلب: $orderId');
    
    if (context.mounted) {
      // ✅ التنقل باستخدام GoRouter
      context.go('/orders/details/$orderId');
      debugPrint('✅ تم التنقل إلى صفحة الطلب بنجاح: $orderId');
    }
  } catch (e) {
    debugPrint('❌ خطأ في تنفيذ التنقل: $e');
  }
}
```

### 4️⃣ عرض صفحة تفاصيل الطلب

```dart
// frontend/lib/pages/user_order_details_page.dart

class UserOrderDetailsPage extends StatefulWidget {
  final String orderId;

  const UserOrderDetailsPage({required this.orderId});

  @override
  State<UserOrderDetailsPage> createState() => _UserOrderDetailsPageState();
}

class _UserOrderDetailsPageState extends State<UserOrderDetailsPage> {
  @override
  void initState() {
    super.initState();
    debugPrint('📄 فتح صفحة تفاصيل الطلب: ${widget.orderId}');
    _loadOrderDetails();
  }

  Future<void> _loadOrderDetails() async {
    try {
      // جلب تفاصيل الطلب من Backend API
      final order = await OrderDetailsService.fetchOrderDetails(widget.orderId);
      
      setState(() {
        _order = order;
        _isLoading = false;
      });

      debugPrint('✅ تم تحميل تفاصيل الطلب بنجاح');
    } catch (e) {
      debugPrint('❌ خطأ في جلب تفاصيل الطلب: $e');
    }
  }

  @override
  Widget build(BuildContext context) {
    // عرض تفاصيل الطلب
    return Scaffold(
      appBar: AppBar(title: const Text('تفاصيل الطلب')),
      body: _buildOrderContent(),
    );
  }
}
```

---

## 📊 مخطط سير العملية

```
┌─────────────────────────────────────────────────────────────────┐
│                    Backend (Node.js)                            │
│                                                                 │
│  integrated_waseet_sync.js                                      │
│  ├─ تحديث حالة الطلب                                           │
│  ├─ فحص الحالة المسموحة                                        │
│  └─ إرسال الإشعار عبر Firebase                                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              Firebase Cloud Messaging (FCM)                     │
│                                                                 │
│  ✅ الإشعار يحتوي على:                                         │
│  - title: "🚗 قيد التوصيل"                                     │
│  - body: "أحمد محمد - (قيد التوصيل)"                           │
│  - data.orderId: "order-123"                                    │
│  - data.type: "order_status_update"                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              Flutter App (Android/iOS)                          │
│                                                                 │
│  FCM Service                                                    │
│  ├─ استقبال الإشعار                                           │
│  ├─ معالجة البيانات                                           │
│  └─ استخراج معرف الطلب                                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              معالجة النقر على الإشعار                          │
│                                                                 │
│  _processNotificationData()                                     │
│  ├─ استخراج orderId من البيانات                               │
│  ├─ التحقق من صحة البيانات                                    │
│  └─ استدعاء _navigateToOrderDetails()                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              التنقل باستخدام GoRouter                          │
│                                                                 │
│  _navigateToOrderDetails()                                      │
│  ├─ الحصول على BuildContext                                   │
│  ├─ التحقق من توفر Context                                    │
│  └─ استدعاء _performNavigation()                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              تنفيذ التنقل                                       │
│                                                                 │
│  _performNavigation()                                           │
│  ├─ context.go('/orders/details/$orderId')                      │
│  └─ التنقل إلى صفحة تفاصيل الطلب                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              صفحة تفاصيل الطلب                                  │
│                                                                 │
│  UserOrderDetailsPage                                           │
│  ├─ جلب تفاصيل الطلب من Backend API                           │
│  ├─ عرض الحالة الجديدة                                        │
│  ├─ عرض بيانات العميل                                        │
│  └─ عرض عناصر الطلب                                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 السيناريوهات الثلاثة

### السيناريو 1️⃣: التطبيق في المقدمة
```
الإشعار يصل → عرض محلي → النقر → معالجة البيانات → التنقل
```

### السيناريو 2️⃣: التطبيق في الخلفية
```
الإشعار يصل → النقر → فتح التطبيق → معالجة البيانات → التنقل
```

### السيناريو 3️⃣: التطبيق مغلق
```
الإشعار يصل → النقر → فتح التطبيق من الصفر → معالجة البيانات → التنقل
```

---

## ✅ قائمة التحقق

- [x] Backend يرسل معرف الطلب في بيانات الإشعار
- [x] FCM Service يستقبل الإشعار بشكل صحيح
- [x] معالجة البيانات تستخرج معرف الطلب
- [x] التنقل يستخدم GoRouter
- [x] صفحة تفاصيل الطلب تحمل البيانات بشكل صحيح
- [x] جميع السيناريوهات الثلاثة تعمل

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا يتم التنقل عند النقر على الإشعار

**الحل:**
1. تحقق من وجود `orderId` في بيانات الإشعار
2. تحقق من سجلات التطبيق (debugPrint)
3. تأكد من أن GoRouter مهيأ بشكل صحيح
4. تحقق من أن المسار `/orders/details/:orderId` موجود في router.dart

### المشكلة: صفحة تفاصيل الطلب فارغة

**الحل:**
1. تحقق من أن معرف الطلب صحيح
2. تحقق من Backend API
3. تحقق من الأذونات والمصادقة
4. تحقق من سجلات الشبكة

---

## 📝 الملفات المتعلقة

- `frontend/lib/services/fcm_service.dart` - خدمة FCM
- `frontend/lib/router.dart` - تعريف المسارات
- `frontend/lib/pages/user_order_details_page.dart` - صفحة تفاصيل الطلب
- `backend/services/firebase_admin_service.js` - إرسال الإشعارات
- `backend/services/integrated_waseet_sync.js` - مزامنة الحالات

